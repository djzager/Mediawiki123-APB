---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ app_name }}
  namespace: {{ namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ mediawiki_volume_size }}
